CREATE TABLE irctc_stream_tb (
  row_key VARCHAR(MAX),
  name VARCHAR(MAX),
  age VARCHAR(MAX),
  email VARCHAR(MAX),
  join_date VARCHAR(MAX),
  last_login VARCHAR(MAX),
  loyalty_points VARCHAR(MAX),
  account_balance VARCHAR(MAX),
  is_active VARCHAR(MAX),
  inserted_at VARCHAR(MAX),
  updated_at VARCHAR(MAX),
  loyalty_status VARCHAR(MAX),
  account_age_days VARCHAR(MAX)
);

